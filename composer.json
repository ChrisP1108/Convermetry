{
    "name": "convermetry/convermetry",
    "description": "Visitor analytics, campaign attribution, and server-confirmed form conversion tracking with reliable webhook delivery.",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=8.3"
    },
    "require-dev": {
        "brain/monkey": "^2.6",
        "phpstan/phpstan": "^2.2",
        "phpunit/phpunit": "^10.5",
        "szepeviktor/phpstan-wordpress": "^2.0"
    },
    "config": {
        "platform": {
            "php": "8.3.0"
        },
        "sort-packages": true
    },
    "autoload": {
        "psr-4": {
            "Convermetry\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Convermetry\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "analyse": "phpstan analyse --memory-limit=1G",
        "test": "phpunit --display-skipped",
        "test:integration": "phpunit -c phpunit.integration.xml --display-skipped",
        "test:wordpress": "phpunit -c phpunit.wordpress.xml --display-skipped",
        "lint": "find src tests convermetry.php uninstall.php -name '*.php' -print0 | xargs -0 -n1 php -l"
    },
    "scripts-descriptions": {
        "analyse": "Static analysis of src/ and both entry points at PHPStan level 8.",
        "test": "The unit suite (Brain Monkey; no WordPress, no database).",
        "test:integration": "The integration suite against a real MySQL server; skips cleanly without one.",
        "lint": "php -l over every tracked PHP file."
    }
}
