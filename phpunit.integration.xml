<?xml version="1.0" encoding="UTF-8"?>
<!--
    Integration suite: runs the plugin's real queries against a real MySQL
    server. Separate from phpunit.xml because it needs a different bootstrap
    (a live $wpdb rather than Brain Monkey stubs).

        vendor/bin/phpunit -c phpunit.integration.xml

    Skips every test cleanly when no database is reachable.
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/Integration/bootstrap.php"
         colors="true"
         failOnWarning="true"
         failOnRisky="true"
         beStrictAboutOutputDuringTests="true"
         cacheDirectory=".phpunit.cache">
    <testsuites>
        <testsuite name="integration">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory suffix=".php">src</directory>
        </include>
    </source>
</phpunit>
